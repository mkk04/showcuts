# Web framework (5.2 is the current LTS, supported into 2028)
Django>=5.2,<6.0

# REST API
djangorestframework>=3.15

# Static file serving in production (gzip/brotli)
whitenoise[brotli]>=6.6

# Outbound HTTP (fetching Shortcuts from iCloud)
requests>=2.31

# Production WSGI server
gunicorn>=22.0

# Database URL parsing (SQLite by default, Postgres via DATABASE_URL)
dj-database-url>=2.1

# PostgreSQL driver (only used when DATABASE_URL points at Postgres)
psycopg[binary]>=3.1

# SCSS -> CSS compilation (build step)
libsass>=0.22

# Load a local .env file in development
python-dotenv>=1.0
